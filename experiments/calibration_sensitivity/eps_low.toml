# HERMES experiments — sensitivity variant: epsilon_bit × 0.5
#
# Derived from experiments/calibration.toml (verified). Only epsilon_bit
# is perturbed; all other constants and provenance unchanged.

[exp1.aerpaw_usrp]
P_idle_W = 5.0
epsilon_bit_J_per_bit = 3.5e-10
B_nominal_bps = 10_000_000

[exp3.mule_platform]
P_idle_W = 5.0
epsilon_bit_J_per_bit = 3.5e-10
epsilon_prop_J_per_m = 10.0
mule_cruise_speed_m_s = 5.0

[provenance]
last_verified = "2026-05-05"
source = "Sensitivity variant of verified calibration: epsilon_bit scaled to 0.5x baseline (7.0e-10 -> 3.5e-10 J/bit). All other values unchanged. See experiments/calibration.toml for primary provenance."
status = "verified"
